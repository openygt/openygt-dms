对架构师建议的 3 项下一步工作，我的意见：                                                                                                    
    ⏭️️   下一步建议                                                                                                                             
  第 1 项（心跳超时检测）和第 2 项（适配器框架 stub）——同意推进。 这两个是 dms-equipment 的 P0/P1                                              
  任务，且不依赖其他模块的未完成工作。心跳检测需要从 SysConfigCache 读取超时阈值（device.heartbeat.idle.timeout /                              
  device.heartbeat.running.timeout），dms-system 的 SysConfig 模块刚完成，正好可以集成验证。                                                   
    3. 状态值中文 → 枚举名迁移（V6 脚本，配合 prod_task.status_enum）
  第 3 项（状态值中文→枚举名迁移）——建议推迟，不在本轮做。 理由：
  - 这是 V6 的工作，当前迭代的 V5 已经预埋了 status_enum 列
  - 77 处代码改动 + 数据库迁移 + 前端适配，风险大、收益在当前迭代不可见
  - dms-production 的状态机重构（synchronized→悲观锁）还没做，先做状态值迁移会导致两个大改动交叉，增加调试难度
                                                                                                                                               
  建议替换第 3 项为：dms-production 状态机核心重构。 这是项目计划中的 P1-3（synchronized→数据库悲观锁）和                                      
  PROD-019，是当前迭代必须完成的高优先级任务，且 dms-equipment 的心跳检测和适配器完成后，production 的设备绑定流程就可以端到端联调了。         
                                                                                                                                               
  推荐的本轮工作安排：                                                                                                                         
                  
  1. dms-equipment：心跳超时检测（HeartbeatCheckScheduler，从 SysConfigCache 读阈值，温度上报同步更新 lastHeartbeat）
  2. dms-equipment：适配器框架 stub（DeviceAdapter 接口 + AdapterRegistry + AbstractMqttAdapter / AbstractTcpBinaryAdapter 基类，东华原实现留
  TODO）
  3. dms-production：状态机并发控制重构（synchronized → selectByIdForUpdate 悲观锁，覆盖 startDecoct/startWrap/bindDevice 三个方法）
                                                                                                                                               
  状态值迁移留到 V6，在 dms-production 重构稳定后再做。
