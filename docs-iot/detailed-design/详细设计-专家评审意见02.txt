● 这份修订报告整体务实，大部分决策我认可。但有几个点需要明确表态。                                                 
                                                                                                                                  
  ---                                                                                                                                          
  认可的部分                                                                                                                                
                                                                                                                                               
  第一节（代码修订 6 项）——全部认可。inspected_at 独立字段、PrintTaskStatus 枚举化、HospitalResponse DTO 屏蔽内部字段、V5 脚本补齐 max_retry 和
   inspected_at，这些都是正确的修复方向。                                                                                                      
                                                                    
  第二节（文档修订 4 项）——认可。加注释标注"当前用中文、V6 改枚举名"是合理的过渡策略。                                                         
                  
  第四节（对评审意见的认可评估）——致命级 100% 认可、高危 85% 认可，这个判断准确。

  ---
  有条件认可的部分
                  
  意见 17~19（getLabel → name / 枚举名存储）推迟到 V6
                                                                                                                                               
  架构师的理由是"77 处中文状态值引用，当前迭代风险过高"。这个判断我有条件认可，但需要补充一个硬性约束：                                        
                                                                                                                                               
  V5 必须在迁移脚本中预埋枚举名列，为 V6 做准备：                                                                                              
                  
  ALTER TABLE prod_task ADD COLUMN status_enum VARCHAR(30);                                                                                    
  UPDATE prod_task SET status_enum = CASE status
      WHEN '待泡药' THEN 'WAIT_SOAK'
      WHEN '泡药中' THEN 'SOAKING'
      -- ... 全部 14 个状态
  END;

  不需要当前迭代切换读写，但列和数据必须就位。否则 V6 迁移时仍然要面对"一次性改 77 处"的风险，只是把炸弹往后推了一个版本。                     
   
  如果不愿意加列，至少在 V5 迁移脚本中加一段数据校验 SQL，确认所有 prod_task.status 值都能映射到 TaskStatus 枚举的某个 getLabel()，防止脏数据在
   V6 迁移时爆炸。
                                                                                                                                               
  意见 21（QualityService.getInspectionSummary 参数 Date）

  报告说"已在第一轮修复，参数改为 Date"。但 dms-common 全局规范是 LocalDateTime，这里用 Date 仍然不一致。既然这个方法当前无调用方，改成        
  LocalDateTime 零风险。建议趁现在改掉，不要留到 V6。
                                                                                                                                               
  ---             
  不认可的部分
              
  意见 20（@TransactionalEventListener）——"代码中尚无 Spring Event 机制"不是保留理由
                                                                                                                                               
  dms-print-详细设计.md 第 820-865 行已经设计了 PrintTaskSubmittedEvent + PrintTaskEventListener + @EventListener。这不是"尚无 Spring Event    
  机制"，而是详设已经引入了 Event 机制但用错了注解。                                                                                           
                                                                                                                                               
  如果按当前详设实现，submitPrintTask() 在 @Transactional 事务中 publishEvent，@EventListener 在事务提交前同步触发，异步线程可能读到未提交的   
  prt_task 记录。这是一个确定性 bug，不是架构增强。
                                                                                                                                               
  修订要求：将详设中的 @EventListener 改为 @TransactionalEventListener(phase = 
  TransactionPhase.AFTER_COMMIT)。这是一行注解的改动，不涉及架构变更。

  意见 23（AOP 代理不生效）——"已改为纯 SPI 调用，无 @Async 内部方法调用"需要验证

  架构师说"代码中 PrintServiceImpl 已改为纯 SPI 调用，无 @Async 内部方法调用"。但详设文档（第 673 行）handlePrintTaskEvent 方法仍然标注了      
  @Async("printExecutor") 和 @Transactional，且 PrintTaskEventListener（第 863 行）仍然通过 printServiceImpl.handlePrintTaskEvent(event)
  调用。如果代码已经改了但文档没同步，那文档需要更新；如果代码没改，这个问题仍然存在。                                                         
                  
  修订要求：要么更新详设文档反映代码实际状态，要么将 handlePrintTaskEvent 抽到独立的 PrintAsyncExecutor Bean 中。                              
   
  ---                                                                                                                                          
  遗漏的关键问题  

  架构师的报告没有提及以下仍未修复的致命/高危问题：

  ┌────────────┬──────────────────────────────────────────────┬──────┐
  │    编号    │                     问题                     │ 状态 │
  ├────────────┼──────────────────────────────────────────────┼──────┤
  │ 原 1-3     │ dms-equipment 包名 com.openygt.dms           │ 未修 │
  ├────────────┼──────────────────────────────────────────────┼──────┤
  │ 原 1-4/5   │ dms-equipment JPA 注解未改 MyBatis-Plus      │ 未修 │
  ├────────────┼──────────────────────────────────────────────┼──────┤                                                                         
  │ 原 1-5     │ dms-equipment EquipmentService 仅 4 方法     │ 未修 │
  ├────────────┼──────────────────────────────────────────────┼──────┤                                                                         
  │ 原 1-6     │ dms-equipment 用 Result<T> 非 ApiResponse<T> │ 未修 │
  ├────────────┼──────────────────────────────────────────────┼──────┤
  │ 原 1-7     │ dms-equipment DeviceStatus 用 ALARM 非 FAULT │ 未修 │
  ├────────────┼──────────────────────────────────────────────┼──────┤
  │ 原 1-2     │ ProductionQueryService 两套定义              │ 未修 │
  ├────────────┼──────────────────────────────────────────────┼──────┤
  │ 原 1-19/20 │ JWT Filter 不阻断 + 默认密钥校验             │ 未修 │
  ├────────────┼──────────────────────────────────────────────┼──────┤
  │ 原 1-14    │ BCryptPasswordEncoder Bean 未注册            │ 未修 │
  ├────────────┼──────────────────────────────────────────────┼──────┤
  │ 原 1-1     │ dms-production 实体 Date → LocalDateTime     │ 未修 │
  └────────────┴──────────────────────────────────────────────┴──────┘

  这些问题在修订报告中既没有标记为"已修"，也没有标记为"保留"。需要架构师明确每一条的处置状态。                                                 
   
  特别是 dms-equipment-详细设计.md——上两轮评审都指出这份文档需要整体重写（包名、ORM、SPI、枚举、响应类 5                                       
  个维度全部不一致），但修订报告中完全没有提及。这是当前最大的阻塞项。
                                                                                                                                               
  ---             
  结论

  ┌──────────────────────────┬────────────────────────────────────────────────────┐
  │           维度           │                        判定                        │
  ├──────────────────────────┼────────────────────────────────────────────────────┤
  │ 代码修订 6 项            │ 认可                                               │
  ├──────────────────────────┼────────────────────────────────────────────────────┤
  │ 文档修订 4 项            │ 认可                                               │
  ├──────────────────────────┼────────────────────────────────────────────────────┤
  │ 状态值推迟 V6            │ 有条件认可（需预埋枚举名列或校验 SQL）             │
  ├──────────────────────────┼────────────────────────────────────────────────────┤                                                            
  │ 意见 20 保留             │ 不认可，@TransactionalEventListener 必须当前迭代修 │
  ├──────────────────────────┼────────────────────────────────────────────────────┤                                                            
  │ 意见 23 保留             │ 需要验证代码与文档是否一致                         │
  ├──────────────────────────┼────────────────────────────────────────────────────┤
  │ dms-equipment 整体重写   │ 报告未提及，需要补充处置方案                       │
  ├──────────────────────────┼────────────────────────────────────────────────────┤
  │ dms-production 实体 Date │ 报告未提及，需要补充处置方案                       │
  └──────────────────────────┴────────────────────────────────────────────────────┘

  建议架构师补充一份 dms-equipment-详细设计.md 的修订计划，以及对上述 9 条遗漏问题的逐条处置说明，然后可以进入 Sprint 开发。    