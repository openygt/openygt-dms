# 测试环境专用配置（使用 MySQL，与生产一致）
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/openygt-dms?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: mysql_pwd01
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000
  redis:
    host: 127.0.0.1
    port: 6379
    password: Yaoliangxin1

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

mqtt:
  broker-url: tcp://127.0.0.1:1883
  client-id: dms-backend-test
  topic-subscription: "+/+/+"

jwt:
  secret: Test-Only-JWT-Secret-Key-For-Regression-32Bytes!

logging:
  level:
    cn.org.openygt: DEBUG
    root: INFO
