# OpenYGT-DMS 架构设计

## 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                         用户层                                │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ Web 管理端 │  │ PDA 移动端 │  │  设备端   │  │ 打印机   │   │
│  │  Vue 3   │  │ uni-app  │  │  MQTT   │  │  串口   │   │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘   │
└───────┼─────────────┼─────────────┼─────────────┼──────────┘
        │             │             │             │
        ▼             ▼             ▼             ▼
┌─────────────────────────────────────────────────────────────┐
│                      网关/接入层                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │              Nginx 反向代理 + 负载均衡                   │ │
│  └────────────────────────────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────────────┐ │
│  │              IoT 网关（MQTT Broker）                     │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│                      应用服务层                               │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐           │
│  │  生产管理  │ │  设备管理  │ │  质量检验  │ │  发药管理  │           │
│  │ dms-prod │ │ dms-eq  │ │dms-quality│ │dms-print │           │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘           │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐           │
│  │  库存管理  │ │  系统管理  │ │  主数据   │ │  门诊管理  │           │
│  │dms-inventory│ │dms-system │ │dms-master │ │dms-outpatient│           │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘           │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐                      │
│  │  权限控制  │ │  数据分析  │ │  IoT 网关  │                      │
│  │ dms-rbac │ │dms-analytics│ │dms-iot   │                      │
│  └─────────┘ └─────────┘ └─────────┘                      │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│                      数据存储层                               │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │    MySQL     │  │    Redis     │  │   文件存储   │         │
│  │  业务数据    │  │  缓存/会话   │  │  上传文件   │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
└─────────────────────────────────────────────────────────────┘
```

## 模块说明

| 模块 | 职责 | 技术要点 |
|------|------|----------|
| dms-app | 应用启动入口、统一配置 | Spring Boot Starter |
| dms-common | 公共工具类、常量、异常 | 无业务逻辑 |
| dms-system | 用户、角色、菜单、权限、参数 | RBAC 权限模型 |
| dms-rbac | 权限控制核心 | JWT + 接口级权限 |
| dms-production | 处方管理、任务调度、生产看板 | 状态机驱动 |
| dms-equipment | 设备台账、监控、告警、分组 | MQTT 实时通信 |
| dms-quality | 质检记录、留样、合格判定 | 质量追溯链 |
| dms-inventory | 成品暂存、出入库 | 库存事务 |
| dms-print | 标签打印、工单打印 | 打印模板引擎 |
| dms-masterdata | 医院、科室、医生、药品 | 主数据标准 |
| dms-analytics | 数据看板、统计报表 | 实时 + 离线统计 |
| dms-iot-gateway | 设备协议适配、消息路由 | MQTT Broker |

## 技术架构特点

1. **模块化设计**：基于 Maven 多模块，各模块可独立编译部署
2. **前后端分离**：RESTful API + JWT 认证
3. **数据库版本管理**：Flyway 自动迁移，支持多环境
4. **设备物联网**：MQTT 协议，支持多品牌煎药机、包装机对接
5. **移动端支持**：uni-app 跨平台，支持 H5 和 Android PDA

## 数据库设计

- 逻辑删除：所有业务表支持 `deleted` 字段软删除
- 多租户：预留 `tenant_id` 字段，支持未来多机构部署
- 审计字段：`created_at`、`updated_at` 自动填充
